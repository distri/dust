.options
  %h2 Options
  .option
    .key Volume
    %input(type="range" value=@volume min=0 max=1 step="0.1")
      - on "change", @volume # TODO: HamlJr should auto handle this through the value attribute binding
